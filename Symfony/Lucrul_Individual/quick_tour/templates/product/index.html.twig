{% extends 'base.html.twig' %}

{% block title %}Hello !{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    table, th , td  {
  border: 1px solid grey;
  border-collapse: collapse;
  padding: 5px;
}
table tr:nth-child(odd) {
  background-color: #f1f1f1;
}
table tr:nth-child(even) {
  background-color: #ffffff;
}
</style>

<div class="example-wrapper">

Tabelul products:
<table>
<th>ID</th>
<th>Nume</th>
<th>pret</th>
<th>descriere</th>
<th>categoria</th>
<th>Actiuni</th>
{% for product in products %}
   <tr>
       <td> {{ product.id }} </td>

    <td> {{ product.name }} </td>
      <td> {{ product.price }} </td>
        <td>*{{ product.description }} </td>
          <td> {{ product.category.name }} </td>
                  <td>
            <div class="d-flex justify-content-around">
            <a href="{{path('edit_product',{id:product.id})}}">Update</a>
            <a class="text-danger" href="{{path('delete_product',{id:product.id})}}">Delete</a>
            </div>
        </td>
    </tr>
{% else %}
    No users have been found.
{% endfor %}
</table>


<form action="{{path('create_product')}}">
    <button type="submit" class="btn btn-primary btn-lg btn-block">Creaza Product Nou</button>
</form>

</div>
{% endblock %}
